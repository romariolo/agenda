spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agenda
    username: romario
    password: 4577
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
logging:
  level:
    org.springframework.web: DEBUG
    com.seuprojeto.agendamento: DEBUG
